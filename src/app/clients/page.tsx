import Sidebar from "@/components/MenuPanel";

const ClientsPage = () => (
  <div className="container">
    <Sidebar />
    <main>
      <h1>Clientes</h1>
    </main>
  </div>
);

export default ClientsPage;
